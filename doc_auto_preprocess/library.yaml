---
title: preprocess
version: 1.0.0
description:  Run Preprocess for Document Automation
icon: python

tags:
  - training

command: python preprocess_dataset.py
requirements:
  cpu: 64
  memory: 64
  gpu: 0
  hpu: 0

image: ai-workflows:beta-doc-automation-fine-tuning
language: python3

arguments:
  - key: data_dir
    type: 'categorical'
    values:
      - '/input/doc_auto_prepare_data/sampled_dataset'
  - key: save_to
    type: 'categorical'
    values:
      - 'output/processed_data/'
  - key: train_file
    type: 'categorical'
    values:
      - 'docvqa.json'

      